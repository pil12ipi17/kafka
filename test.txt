Тестовое задание:
Kafka в микросервисах (cp-demo + Python-практика)КонтекстПрактическое задание по Kafka-экосистеме в формате реального стенда.
Фокус задания:
СценарийКоманда планирует использовать event-driven подход для микросервисов.
Нужно быстро выйти на уровень, когда можно:
В качестве базового полигона используется официальный стенд Confluent cp-demo.
Общая задачаВыполнить задание в двух независимых блоках:
Блок A.
Развернуть и изучить cp-demoНужно поднять стенд, пройтись по потоку данных и зафиксировать, как именно он работает.
Блок B.
Python-флекс поверх стендаНужно добавить/адаптировать Python-микросервисы для продюсинга и потребления событий, а также усилить пайплайн по надежностии управлению схемами.
Блок A.
Развернуть и изучить cp-demo (обязательный)A1) Локальный запуск стендаТребуется:
Минимальный список сервисов для проверки:
A2) Понять входной поток данныхТребуется:
Ожидается, что будет явно описано:
понять end-to-end поток данных в Kafka-инфраструктуренаучиться уверенно работать с Python-клиентами Kafka в микросервисном сценариичитать и диагностировать существующий streaming-пайплайндобавлять Python-сервисы без поломки текущего контуравнедрять базовые механики надежности (retry, DLQ, идемпотентность, схемы)поднять инфраструктуру проекта через docker composeубедиться, что ключевые компоненты healthyпроверить доступ к Control Center и KibanaKafka brokerSchema RegistryKafka ConnectksqlDBControl CenterElasticsearch + Kibanaопределить источник событий в стендезафиксировать путь данных от входа до визуализацииуказать, какие трансформации выполняются по дороге
A3) Карта пайплайнаСобрать компактную карту потока (в draw.io или аналог):
A4) Проверка рукамиПодтвердить работоспособность стенда на практических шагах:
Блок B.
Python-флекс поверх стенда (обязательный)B1) Python producer + consumerРеализовать минимум два Python-сервиса:
Требования:
B2) Контракты событий и Schema RegistryДобавить работу со схемами (Avro/JSON Schema/Protobuf — на выбор):
Ожидается:
B3) Надежность обработкиРеализовать обязательный минимум:
Важно:
B4) Производительность и партиционированиеПровести небольшой практический эксперимент:
какие топики участвуютгде и как подключен source connectorгде и как работает sink connectorисточник данныхingest в Kafkaобработка (ksqlDB/Kafka Streams)выгрузка в Elasticsearchвизуализация в Kibanaпоказать, что события реально поступаютпоказать, что события проходят обработкупоказать, что результат отображается в конечной системеpython-producer-service публикует события в выделенный topicpython-consumer-service читает события, валидирует и публикует результат в следующий topicиспользовать confluent-kafka (или совместимый клиент)на уровне producer использовать key-based routingна уровне consumer использовать контролируемый commitминимум 2 версии одного события (v1 и v2)проверка совместимости схемобработка эволюции поля (добавление/nullable/default)документированный payload contractпример совместимого измененияпример реакции потребителя на новую версиюretry для временных ошибок (с backoff)DLQ topic для неисправимых сообщенийидемпотентная обработка на consumer-сторонеповторное чтение не должно приводить к повторному побочному эффектупричина отправки в DLQ должна логироваться явносравнить обработку при N=1 и N>1 partitions
B5) Наблюдаемость Python-цепочкиДобавить базовую наблюдаемость для новых сервисов:
Ограничения и условияЧто нужно сдатьКлючевые артефакты:
В README обязательно показать:
Критерии приёмкиМинимальный проходной уровеньСильный уровеньТехнические требованияпоказать влияние ключа сообщения на распределениезафиксировать влияние на throughput/latency (приближённо)структурированные логи (event_id, trace_id/correlation_id, topic, partition, offset)минимум 3 метрики (processed, failed, dlq_sent)простой dashboard/снимки метрик (или выгрузка через /metrics)блоки A и B выполняются как независимые части одного заданияизменения в стенде должны быть воспроизводимы локальносекреты и ключи не хардкодитьвсе дополнительные сервисы поднимаются через docker composeрешение должно запускаться на чистой машине по READMEсхема Kafka-пайплайнаdocker-compose.
yml (или override), включающий Python-сервисыкод Python producer/consumerконфиги/артефакты работы со схемамиREADME с пошаговой проверкой блоков A и Bкак поднять cp-demoкак проверить путь данных от входа до витриныкак запустить Python-сервисыкак проверить retry, DLQ и идемпотентностькак проверить сценарий с эволюцией схемстенд cp-demo запущен и подтверждён рабочимPython producer/consumer публикуют и читают событияесть DLQ и retryесть хотя бы один сценарий эволюции схемкачественная схема и ясное описание потокааккуратная обработка ошибок и идемпотентностьпонятная диагностика через логи и метрикиаргументированный выбор ключей, партиций и commit-стратегииPython 3.11+Docker / docker composeApache Kafka (в составе cp-demo)confluent-kafka (Python)
Можно использовать:
Где смотреть теорию (практичные ресурсы)ВажноВ этом задании оценивается не только «сервис читает и пишет в Kafka», а инженерная готовность работать с реальным event-drivenконтуром:
Schema RegistryksqlDB / Kafka Connect (из стенда)pydantic для валидации payloadtenacity для retryprometheus-client для метрикcp-demo: https://github.com/confluentinc/cp-demoConfluent examples: https://github.com/confluentinc/examplesKafka docs (latest): https://kafka.apache.org/42/documentation.htmlSchema Registry fundamentals: https://developer.confluent.io/courses/schema-registry/Kafka Connect docs: https://docs.confluent.io/platform/current/connect/Kafka Streams docs: https://docs.confluent.io/platform/current/streams/понимание end-to-end потокаустойчивость обработкиконтроль контрактов событийвоспроизводимость и удобство поддержки